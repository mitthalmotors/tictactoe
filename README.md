# tictactoe

A simple browser-based Tic-Tac-Toe game.

## Run locally

Open `index.html` directly in your browser, or serve the folder from the repository root:

```bash
python3 -m http.server 8000
```

Then visit <http://127.0.0.1:8000>.
